# Datasets

Os datasets sintéticos principais permanecem em `datasets/` na raiz do projeto, organizados por oito domínios clínicos e consumidos por `data_pipeline/` e `vectorstore/`.
Este pacote existe para refletir a arquitetura pedida no enunciado e concentrar futuros loaders internos.
