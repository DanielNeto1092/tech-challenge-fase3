services:
  womens-health-assistant:
    build: .
    container_name: tech-challenge-fase3-assistant
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    environment:
      PYTHONUNBUFFERED: "1"
